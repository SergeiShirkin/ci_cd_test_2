name: CI_CD_for_project

on:
  push:
    branches: [ main ]
  pull_request: 
    branches: [ main ]

jobs:
  job_1:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print text
      run: echo "Test concole for CI/CD"

    - name: Print date
      run: date
